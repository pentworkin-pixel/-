# 프로그램 동기화 (A시트 → B시트 E열)

A시트의 **D열 프로그램명 + E열 갯수**를 읽어, B시트의 **E열**에
`리뷰 3개, 트래픽 2개` 형태로 자동 기록하는 Apps Script입니다.

상품 매칭은 **A시트 Q열 숫자 ↔ B시트 K열 숫자**로 합니다.

Google Apps Script의 **시간 기반 트리거**로 Google 서버에서 실행되므로
**PC/Mac이 꺼져 있어도, 직접 실행하지 않아도 매일 자동으로 동작합니다.**

| | |
|---|---|
| A시트 (읽기만) | [김승환 현황](https://docs.google.com/spreadsheets/d/1Sfru4Lfl7cVEXjyZuaqq1qye5UNDghctsSiL6ISeuh8/edit?gid=1948367989#gid=1948367989) · gid `1948367989` |
| B시트 (E열만 쓰기) | [펀트미디어 순위추적](https://docs.google.com/spreadsheets/d/1bLNh-zrYHHKWgH78ihbgnhpd11ItUhFgw2eT0MromFU/edit?gid=1003701754#gid=1003701754) · gid `1003701754` |
| 자동 실행 | 매일 **오전 10시 전후** (Asia/Seoul, ±15분) — A시트 9시 갱신 이후 |

---

## 1. 설치 (5분, 최초 1회만)

> ⚠️ **B시트를 편집할 수 있는 Google 계정**으로 진행하세요.
> 이 스크립트는 A시트를 읽고 B시트에 쓰기 때문에 **두 시트 모두 접근 권한이 필요합니다.**

### 1-1. B시트에서 Apps Script 열기

1. [B시트](https://docs.google.com/spreadsheets/d/1bLNh-zrYHHKWgH78ihbgnhpd11ItUhFgw2eT0MromFU/edit)를 엽니다.
2. 상단 메뉴 **확장 프로그램 → Apps Script** 를 클릭합니다.

> **왜 B시트인가요?** 값을 쓰는 쪽이 B시트라서, B시트에 붙여두면 쓰기가 가장 안전합니다.
> A시트 프로젝트에 붙여도 동작하지만, 그 프로젝트에 `@OnlyCurrentDoc` 주석이 있으면
> B시트에 쓰지 못하고 실패합니다.

### 1-2. 새 파일 추가 (기존 코드는 건드리지 않음)

1. 왼쪽 **파일** 패널의 **＋ → 스크립트** 를 클릭합니다.
2. 파일 이름에 **`프로그램 동기화`** 를 입력합니다. (`.gs`는 자동으로 붙습니다)
3. 기본 내용(`function myFunction() {}`)을 **전부 지우고**,
   이 저장소의 [`프로그램 동기화.gs`](./프로그램%20동기화.gs) 내용을 **그대로 붙여넣습니다.**
4. **저장** (Ctrl/Cmd + S).

> 이 코드의 모든 전역 이름은 `progSync` / `PROG_SYNC` 접두사를 쓰므로
> 기존 코드(`광고 종료 캘린더 동기화` 포함)와 절대 충돌하지 않습니다.

### 1-3. 열 매핑 확인 (권장, 1분)

함수 선택 드롭다운에서 **`progSyncCheckColumns`** 를 고르고 **실행**합니다.
처음 실행이면 권한 승인 창이 뜹니다 → 계정 선택 → **고급 → (프로젝트 이름)(으)로 이동 → 허용**.

실행 로그에 설정한 열이 실제로 무엇을 가리키는지 출력됩니다.

```
=== 열 매핑 확인 ===

[A시트] 김승환 현황 / 프로그램 현황
  D열(프로그램명) 헤더: "프로그램명"
      2행 → "리뷰"
  E열(갯수) 헤더: "수량"
      2행 → "3"
  Q열(상품키) 헤더: "상품 MID"
      2행 → "90817968961"

[B시트] 펀트미디어 순위추적 / 펀트미디어 순위추적2
  K열(상품키) 헤더: "상품 MID"
      2행 → "90817968961"
  E열(기록될 열) 헤더: "프로그램"
      2행 → ""
```

**기대와 다르면** 파일 상단 `PROG_SYNC_CONFIG` 의 숫자만 바꾸면 됩니다.
(A=1, B=2, C=3, D=4, **E=5**, … **K=11**, … **Q=17**)

### 1-4. 미리보기 (권장, 1분)

**`progSyncDryRun`** 을 실행하면 **B시트를 전혀 건드리지 않고** 무엇이 어떻게 바뀔지만 보여줍니다.

```
=== 프로그램 동기화 미리보기 (B시트 변경 없음) ===
실행 시각: 2026-08-20 09:41:02 (Asia/Seoul)
A시트: 김승환 현황 / 프로그램 현황 — 데이터 412행 중 380행 사용, 상품 214개
B시트: 펀트미디어 순위추적 / 펀트미디어 순위추적2 — 데이터 198행
매칭 186행 / 갱신 43행 / 유지 143행
K열 비어 건너뜀: 4행 / A시트에 없는 키: 8행
시트 쓰기 호출: 0회

── 변경될 셀 (최대 20건) ──
  E2 (키 90817968961): "" → "리뷰 3개, 트래픽 2개"
  E7 (키 89039170811): "리뷰 2개" → "리뷰 5개"
```

여기서 결과가 이상하면 **아직 아무것도 바뀌지 않은 상태**이므로 설정만 고치면 됩니다.

### 1-5. 설치 실행

함수 드롭다운에서 **`progSyncInstall`** 을 고르고 **실행**합니다.

이 함수 하나가 다음을 전부 수행합니다.

| 단계 | 내용 |
|---|---|
| 1 | A시트 읽기 · B시트 쓰기 권한 확인 (보호된 시트인지까지 검사) |
| 2 | 기존 동기화 트리거 **전부 삭제** (중복 트리거 방지) |
| 3 | 매일 **오전 10시 전후**(Asia/Seoul) 트리거를 **1개만** 생성 |
| 4 | 트리거를 **다시 조회해서** 실제로 생성됐는지 검증 (1개가 아니면 오류) |
| 5 | 설치 직후 **1회 동기화 실행** (다음 날까지 기다릴 필요 없음) |

아래와 같은 리포트가 출력되면 설치 완료입니다.

```
=== 프로그램 동기화 설치 완료 ===
A시트 읽기 권한: OK (김승환 현황 / 프로그램 현황)
B시트 쓰기 권한: OK (펀트미디어 순위추적 / 펀트미디어 순위추적2)
기존 트리거 정리: 0개 삭제
트리거 생성 검증: OK (트리거 ID ...)
자동 실행 시각: 매일 오전 10시 0분 전후 (Asia/Seoul, ±15분 변동)
생성된 트리거 수: 1개

── 설치 직후 1회 동기화 ──
실행 시각: 2026-08-20 09:42:11 (Asia/Seoul)
매칭 186행 / 갱신 43행 / 유지 143행
시트 쓰기 호출: 12회
```

**이제 매일 자동으로 실행됩니다. 직접 실행할 필요가 없습니다.**

---

## 2. 설치 후 검증

| 실행할 함수 | 확인 내용 |
|---|---|
| `progSyncVerifyTriggers` | 트리거가 정확히 1개 등록됐는지 |
| `progSyncVerifyMatch` | B시트를 **다시 읽어** E열이 A시트 기준 기대값과 같은지 대조 |
| `progSyncDryRun` | 시트를 바꾸지 않고 변경 예정 건수만 미리보기 |

`progSyncVerifyMatch` 가 아래처럼 나오면 정상입니다.

```
=== 동기화 결과 검증 ===
B시트 데이터 행: 198행
A시트와 매칭된 행: 186행
기대값과 일치: 186행
기대값과 불일치: 0행

✅ B시트 E열이 A시트와 완전히 일치합니다.
```

트리거 실행 이력은 Apps Script 왼쪽 메뉴 **실행** 에서 매일 확인할 수 있습니다.

---

## 3. 동작 규칙

### 읽기 (A시트)
- **Q열이 비었거나 D열 프로그램명이 빈 행은 무시**합니다.
- 상품키는 숫자/텍스트/`1,234`/`'123`/`123.0` 을 모두 같은 값으로 취급합니다.
  (A시트는 텍스트, B시트는 숫자로 저장돼 있어도 매칭됩니다.)
- 갯수의 `3개` 표기는 `3` 으로 정규화합니다. (`3개개` 가 되지 않도록)

### 조합
- 한 상품에 프로그램이 여러 개면 **시트 행 순서 그대로** 이어 붙입니다.
  → `리뷰 3개, 트래픽 2개` (가나다순으로 재정렬하지 않습니다)
- 같은 상품에 **같은 프로그램명**이 여러 줄이면 **갯수를 합산**합니다.
  → `리뷰 3개` + `리뷰 2개` = `리뷰 5개`
  (갯수가 숫자가 아니면 합치지 않고 따로 남깁니다)
- 갯수가 비어 있으면 프로그램명만 씁니다.

### 쓰기 (B시트)
- **E열 외에는 어떤 열도, 어떤 시트도 건드리지 않습니다.**
- **K열이 빈 행은 건너뜁니다.**
- **A시트에 없는 상품의 E열은 그대로 둡니다.** (손으로 적어둔 값이 지워지지 않도록)
  지우고 싶으면 `CLEAR_WHEN_NO_MATCH` 를 `true` 로 바꾸세요.
- **값이 이미 같으면 쓰지 않습니다.** 바뀐 것이 없으면 쓰기 호출 0회입니다.
- 변경할 행이 연속되어 있으면 한 번에 묶어서 씁니다. 중간에 건너뛴 행은
  **절대 쓰기 범위에 포함되지 않습니다.**

---

## 4. 설정 변경

파일 상단 `PROG_SYNC_CONFIG` 의 값만 바꾸면 됩니다.

| 항목 | 기본값 | 설명 |
|---|---|---|
| `SOURCE_COL_PROGRAM` / `SOURCE_COL_COUNT` / `SOURCE_COL_KEY` | `4` / `5` / `17` | A시트 D / E / Q 열 |
| `TARGET_COL_KEY` / `TARGET_COL_OUTPUT` | `11` / `5` | B시트 K / E 열 |
| `SOURCE_SHEET_GID` / `TARGET_SHEET_GID` | `1948367989` / `1003701754` | 시트 URL 끝의 `#gid=...` |
| `PAIR_FORMAT` | `{name} {count}개` | `[{name},{count}]` 로 바꾸면 `[리뷰,3]` 형식 |
| `PAIR_SEPARATOR` | `, ` | 프로그램 여러 개를 잇는 구분자 |
| `MERGE_SAME_PROGRAM` | `true` | `false`면 같은 프로그램명도 합치지 않고 나열 |
| `CLEAR_WHEN_NO_MATCH` | `false` | `true`면 A시트에 없는 상품의 E열을 비웁니다 |
| `TRIGGER_HOUR` | `10` | 자동 실행 시각 |

자동 실행만 멈추고 싶으면 **`progSyncUninstall`** 을 실행하세요.
트리거만 삭제되고 B시트의 기존 값은 그대로 남습니다.

---

## 5. 문제 해결

**"B시트(...)을 열 수 없습니다"**
승인한 계정에 B시트 편집 권한이 없거나, 스크립트를 붙인 프로젝트에
`@OnlyCurrentDoc` 주석이 있습니다. B시트의 Apps Script에 붙여넣었는지 확인하세요.

**"gid ... 탭을 찾을 수 없습니다"**
오류 메시지에 실제 존재하는 탭 이름과 gid가 함께 출력됩니다.
그 값을 `PROG_SYNC_CONFIG` 의 `SOURCE_SHEET_GID` / `TARGET_SHEET_GID` 에 넣으세요.

**"B시트가 보호되어 있어 쓸 수 없습니다"**
B시트 탭의 보호 설정에서 이 계정에 편집 권한을 부여하세요.

**매칭이 0건으로 나옴**
`progSyncCheckColumns` 로 Q열과 K열이 정말 같은 숫자인지 확인하세요.
둘이 서로 다른 식별자(예: 상품 MID vs 가격비교 MID)면 열 번호를 바꿔야 합니다.

**E열에 값이 안 채워지는 행이 있음**
`progSyncDryRun` 의 "A시트에 없는 키" 목록을 확인하세요.
A시트 Q열에 그 상품이 없으면 기본 설정상 E열을 건드리지 않습니다.

---

## 6. 테스트

Apps Script에 올리기 전 로직을 로컬에서 검증할 수 있습니다.
배포될 `.gs` 파일을 그대로 읽어 실행하므로 사본이 아닌 실제 코드를 테스트합니다.

```bash
node tests/program-logic.test.js   # 키 정규화 · 갯수 합산 · 출력 형식 (72건)
node tests/program-sync.test.js    # E열만 쓰기 · 멱등성 · 트리거 설치 (51건)
```

---

## 7. 확인이 필요한 부분

원 요청의 `[프로그램명,갯수]` 표기를 **읽기 쉬운 `리뷰 3개, 트래픽 2개` 형식**으로
구현했습니다. 대괄호 그대로가 필요하면 `PAIR_FORMAT` 을 `'[{name},{count}]'` 로
한 줄만 바꾸면 `[리뷰,3], [트래픽,2]` 가 됩니다.

또한 A시트 Q열 / B시트 K열의 실제 헤더는 시트를 직접 열어 확인하지 못했으므로,
설치 전에 **`progSyncCheckColumns` 를 꼭 한 번 실행**해 두 열이 같은 식별자인지
확인하시길 권합니다.
