# 프로그램별 순위 분석

B시트(순위추적)의 순위 데이터를 읽어, **A스프레드시트**에 「프로그램별 순위 분석」시트를
만들고 프로그램별 반영도를 1일·3일·7일 전과 비교해 정리합니다.

**순위는 숫자가 작을수록 좋습니다.**
`순위 변화폭 = 과거 순위 − 최신 순위` → 양수면 상승, 0이면 유지, 음수면 하락.
(10위 → 3위 = **+7 상승**, 3위 → 3위 = **유지**, 3위 → 10위 = **−7 하락**)

| | |
|---|---|
| 읽는 곳 (B) | [펀트미디어 순위추적](https://docs.google.com/spreadsheets/d/1bLNh-zrYHHKWgH78ihbgnhpd11ItUhFgw2eT0MromFU/edit?gid=1003701754) · gid `1003701754` |
| 쓰는 곳 (A) | [김승환 현황](https://docs.google.com/spreadsheets/d/1Sfru4Lfl7cVEXjyZuaqq1qye5UNDghctsSiL6ISeuh8/edit) — `프로그램별 순위 분석`, `프로그램별 순위 상세` 두 시트만 |
| 실행 | A스프레드시트 상단 **순위 분석 → 프로그램별 순위 분석 업데이트** 메뉴 |

---

## 1. 설치

> 이 스크립트는 **A스프레드시트**에 설치합니다. (메뉴가 A에 떠야 하고, 결과 시트도 A에 생기므로)

### 1-1. 파일 추가

1. [A스프레드시트](https://docs.google.com/spreadsheets/d/1Sfru4Lfl7cVEXjyZuaqq1qye5UNDghctsSiL6ISeuh8/edit) → **확장 프로그램 → Apps Script**
2. **＋ → 스크립트** → 파일 이름 `순위 분석`
3. 기본 내용을 지우고 [`순위 분석.gs`](./순위%20분석.gs) 전체를 붙여넣기 → 저장

> ⚠️ **`onOpen` 충돌 주의**
> 이 프로젝트에 이미 `onOpen` 함수가 있다면 둘 중 하나만 동작합니다.
> 그런 경우 `순위 분석.gs` 안의 `onOpen` 함수를 지우고, **기존 `onOpen` 안에**
> 아래 한 줄만 넣으세요.
> ```js
> rankAnalysisBuildMenu_(SpreadsheetApp.getUi());
> ```

### 1-2. 권한 설정 (appsscript.json)

A스프레드시트 프로젝트에는 **광고 종료 캘린더 동기화**도 들어 있을 수 있습니다.
`oauthScopes`를 직접 적으면 **거기 적힌 권한만** 요청되므로, 기존 스크립트가 쓰는 권한까지
모두 넣어야 기존 기능이 깨지지 않습니다.

프로젝트 설정(⚙️) → **"appsscript.json 매니페스트 파일을 편집기에 표시"** 체크 → 파일 열기:

```json
{
  "timeZone": "Asia/Seoul",
  "dependencies": {
  },
  "exceptionLogging": "STACKDRIVER",
  "runtimeVersion": "V8",
  "oauthScopes": [
    "https://www.googleapis.com/auth/spreadsheets",
    "https://www.googleapis.com/auth/script.container.ui",
    "https://www.googleapis.com/auth/calendar",
    "https://www.googleapis.com/auth/script.scriptapp",
    "https://www.googleapis.com/auth/script.external_request"
  ]
}
```

| 권한 | 누가 쓰는지 |
|---|---|
| `spreadsheets` | 순위 분석 (B 읽기 · A 쓰기) |
| `script.container.ui` | 순위 분석 (메뉴·알림창) |
| `calendar` · `script.scriptapp` · `script.external_request` | 기존 광고 종료 캘린더 동기화 |

> 이 프로젝트에 캘린더 스크립트가 **없다면** 아래 3줄은 빼도 됩니다.
> 반대로 `oauthScopes` 자체를 아예 안 적으면 Apps Script가 알아서 계산하므로
> 그 편이 더 간단할 수도 있습니다. **권한 오류가 날 때만** 위처럼 명시하세요.

`@OnlyCurrentDoc` 주석이 프로젝트 안 어느 파일에든 있으면 B시트를 읽지 못합니다. 지워주세요.

### 1-3. 열 매핑 확인 (먼저 실행)

함수 드롭다운에서 **`rankAnalysisCheckColumns`** → 실행.
처음이면 권한 승인 창이 뜹니다 → 계정 선택 → **고급 → 이동 → 허용**.

```
=== 순위 분석 열 매핑 확인 ===
원본 시트: 펀트미디어 순위추적 / 펀트미디어 순위추적2
데이터 행 수: 198행

인식한 표 형태: 가로형 — 날짜가 열 머리글로 있고 각 칸이 그 날짜의 순위

프로그램명 열: E열 "프로그램"
날짜 열: 7개
   J열 → 2026-07-18 (머리글 "26-07-18")
   ...
   P열 → 2026-08-20 (머리글 "26-08-20")

고유키 구성 열: H열 "상품 MID" + G열 "순위 조회 키워드"
```

**여기서 기대와 다르면 `RANK_ANALYSIS_CONFIG`의 패턴만 고치면 됩니다.**

### 1-4. 실행

A스프레드시트를 새로고침하면 상단에 **순위 분석** 메뉴가 생깁니다.
→ **프로그램별 순위 분석 업데이트** 클릭.

---

## 2. 동작 규칙

### 표 형태 자동 인식

두 가지 형태를 모두 지원합니다.

- **가로형** — 날짜가 열 머리글이고 각 칸이 그 날짜의 순위 (순위추적 시트의 일반적 형태)
- **세로형** — `날짜` 열과 `순위` 열이 따로 있고 한 행이 한 관측

날짜로 읽히는 머리글이 2개 이상이면 가로형으로 봅니다.
`순위 조회 키워드`, `전일 비교 순위(자유 입력)` 같은 열은 순위 열로 오인하지 않도록 걸러냅니다.

### 고유키

**상품 MID + 순위 조회 키워드**를 ` ｜ `로 이어 붙여 만듭니다.

상품명·메모 열은 **일부러 키에서 뺐습니다.** 사람이 수시로 고쳐 쓰는 값이라
글자 하나만 바뀌어도 다른 항목으로 잡혀 비교가 끊기기 때문입니다.
상품 MID 같은 강한 식별자를 못 찾은 경우에만 예비로 이름 열을 씁니다.
그것마저 없으면 행 번호를 키로 씁니다(가로형에서는 행 자체가 항목이므로 유효).

### 비교 날짜 선택

기준일 = B시트의 유효한 날짜 중 **가장 최근 날짜**.

각 기간마다 `기준일 − N일`을 목표로 하되, **정확히 그 날짜가 없으면
목표일보다 이전이면서 가장 가까운 날짜**를 씁니다.

| 상황 | 결과 |
|---|---|
| 기준 9/10, 3일 전 목표 9/7이 있음 | 9/7 사용 |
| 9/7이 없고 9/6이 있음 | **9/6 사용** |
| 9/8만 있음 (목표일보다 나중) | 당겨 쓰지 않음 → **데이터 없음** |
| 과거 데이터가 아예 없음 | **데이터 없음** (억지로 계산하지 않음) |

실제로 사용된 날짜는 분석 시트 상단에 그대로 표시됩니다.

### 상승 / 유지 / 하락 / 신규 / 누락

| 최신 순위 | 과거 순위 | 판정 |
|---|---|---|
| 있음 | 있음 | 변화폭에 따라 **상승 / 유지 / 하락** |
| 있음 | 없음 | **신규** |
| 없음 | 있음 | **누락 또는 종료** |
| 없음 | 없음 | 집계 제외 |

비율의 분모는 **최신·과거 모두 숫자 순위가 있는 "비교 가능 항목 수"**입니다.
신규와 누락은 분모에서 빠집니다.

```
상승률 = 상승 수 ÷ 비교 가능 항목 수
유지율 = 유지 수 ÷ 비교 가능 항목 수
하락률 = 하락 수 ÷ 비교 가능 항목 수
```

### 순위 없음 처리

빈칸, `-`, `미노출`, `순위 없음`, `N/A`, `100+` 등 숫자가 아닌 값은
숫자 순위와 **섞어서 평균내지 않고** 「순위 없음」으로 따로 처리합니다.
`12위`, `1,234` 같은 표기는 숫자로 정상 인식합니다.

### 프로그램 귀속

B시트 E열이 `리뷰 3개, 트래픽 2개`처럼 **여러 프로그램을 담고 있으면 쪼개서
양쪽 모두에 항목을 귀속**시킵니다. (`프로그램 동기화.gs`가 쓰는 형식)

이때 한 항목의 순위 상승을 어느 프로그램 덕분이라고 단정할 수 없으므로,
표 마지막에 **`단독 항목 수`** 열을 뒀습니다. 그 프로그램 **하나만** 붙어 있던
항목이 몇 건인지 보여주므로, 이 숫자가 클수록 해당 프로그램의 성과로 해석하기 안전합니다.

문자열 전체를 하나의 프로그램명으로 보고 싶으면 `SPLIT_PROGRAMS`를 `false`로 바꾸세요.

### 정렬 (반영도 평가)

1. 상승률 높은 순
2. 평균 순위 변화폭 큰 순
3. 하락률 낮은 순
4. 비교 항목 수 많은 순

**비교 가능 항목이 0개인 프로그램은 항상 맨 뒤로 보냅니다.** 비율이 없는데 위로 올라오면
오해를 주기 때문입니다. 표본이 1건인 프로그램이 100% 상승으로 1위에 오를 수는 있으므로,
`비교 가능 항목 수`와 `단독 항목 수`를 반드시 같이 보세요.

### 중복 데이터

같은 날짜 + 같은 고유 항목이 여러 행에 있으면 **가장 마지막 행의 값**을 씁니다.
중복 건수는 실행 로그에 표시됩니다.

---

## 3. 결과 시트 구성

### 「프로그램별 순위 분석」

**상단 요약** (연한 파란 배경)
분석 기준일 / 실제 1·3·7일 전 비교일 / 전체 프로그램 수 / 비교 가능한 전체 항목 수 /
기간별 반영도 1위 / 마지막 분석 실행 시간 / 원본 시트 / 고유키 기준

**기간별 표 3개** (1일 · 3일 · 7일), 각각 16개 열:

순위 · 프로그램명 · 비교 가능 항목 수 · 상승 수 · 유지 수 · 하락 수 · 신규 수 ·
누락 또는 종료 수 · 상승률 · 유지율 · 하락률 · 평균 순위 변화폭 · 평균 상승 폭 ·
평균 하락 폭 · 최신 평균 순위 · **단독 항목 수**

**차트** — 비교 가능한 기간 중 가장 긴 기간의 상승·유지·하락 비율 누적 막대

### 「프로그램별 순위 상세」

프로그램명 · 고유 항목명 · 최신 순위 · 과거 순위 · 순위 변화폭 · 상태 ·
최신 업데이트 날짜 · 실제 비교 날짜 · 비교 기간

### 서식

- 제목 행 굵게, 요약 영역 배경색
- 요약 영역 행 + 순위·프로그램명 열 고정
- 자동 열 너비
- 퍼센트 소수점 1자리 (`0.0%`), 평균값 소수점 2자리 (`0.00`)
- 상승 초록 / 유지 회색 / 하락 빨강 / 신규 파랑
- `평균 순위 변화폭` 조건부 서식: 양수 초록 · 0 회색 · 음수 빨강

> **필터 위치**: Google Sheets는 **시트당 필터를 1개만** 허용합니다.
> 분석 시트에는 표가 3개라 어느 하나에만 걸 수 있어서, 실제로 걸러 볼 일이 많은
> **상세 시트**에 필터를 걸었습니다. 분석 시트의 표는 이미 반영도 순으로 정렬돼 있고
> 프로그램당 한 행이라 필터 없이도 읽을 수 있습니다.

---

## 4. 설정 변경

파일 상단 `RANK_ANALYSIS_CONFIG`:

| 항목 | 기본값 | 설명 |
|---|---|---|
| `SOURCE_SPREADSHEET_ID` | B의 ID | **비우면** A 안에서 `SOURCE_SHEET_NAME`으로 찾습니다 |
| `SOURCE_SHEET_GID` | `1003701754` | 시트 URL 끝의 `#gid=...` |
| `PERIODS` | `[1, 3, 7]` | 비교 기간 |
| `PROGRAM_COL_FALLBACK` | `5` | 프로그램 머리글을 못 찾을 때 쓸 열 (E) |
| `NO_RANK_TOKENS` | `-`, `미노출` 등 | 순위 없음으로 볼 값들 |
| `MAX_VALID_RANK` | `100000` | 이보다 크면 순위로 보지 않음 |
| `SPLIT_PROGRAMS` | `true` | `false`면 E열 문자열 전체가 하나의 프로그램명 |
| `TWO_DIGIT_YEAR_FIRST` | `true` | `26-08-20`을 연-월-일로 해석 |
| `MAX_DETAIL_ROWS` | `5000` | 상세 시트 행 상한 |
| `DRAW_CHART` | `true` | 차트 생성 여부 |

---

## 5. 오류 처리

| 상황 | 동작 |
|---|---|
| B시트를 못 찾음 | 존재하는 탭 이름과 gid를 나열한 오류 |
| 필수 열을 못 찾음 | 무엇을 찾았고 못 찾았는지 알려주는 오류 |
| 날짜 형식 오류 | 그 행만 건너뛰고 건수를 로그에 보고 |
| 순위가 숫자 아님 | 「순위 없음」으로 분리, 평균에 섞지 않음 |
| 프로그램명 비어 있음 | 어느 프로그램에도 귀속시키지 않고 건수만 보고 |
| 중복 데이터 | 마지막 행 채택 + 건수 보고 |
| 비교 날짜 데이터 없음 | 「데이터 없음」 표시, 계산하지 않음 |
| 비교 가능 항목 0개 | 「비교 가능한 항목이 0개입니다」 표시 |

날짜를 하나도 못 찾으면 **분석 시트를 만들지 않고** 오류를 냅니다
(빈 시트가 조용히 생기지 않도록).

---

## 6. 원본 데이터 영향 없음

- 쓰기는 **`프로그램별 순위 분석`, `프로그램별 순위 상세` 두 시트에만** 일어납니다.
- B시트는 `getValues()`로 **읽기만** 합니다.
- 같은 이름의 분석 시트가 이미 있으면 새로 만들지 않고 내용만 갱신합니다.
- 시트를 지우지 않고 `clear()`만 하므로 시트 위치와 링크가 유지됩니다.

이 세 가지는 자동 테스트로 검증됩니다 (`tests/rank-write.test.js`).

---

## 7. 테스트

```bash
node tests/rank-analysis.test.js   # 날짜 선택 · 순위 파싱 · 집계 · 정렬 (136건)
node tests/rank-write.test.js      # 원본 미변경 · 시트 중복 생성 방지 (27건)
```

---

## 8. 확인이 필요한 부분

시트를 직접 열어보지 못한 상태에서 작성했습니다. (Drive 연결 토큰 만료)
**설치 후 `rankAnalysisCheckColumns`를 먼저 실행**해서 아래 두 가지만 확인해 주세요.

1. **프로그램명 열** — E열이 `프로그램 동기화.gs`가 쓰는 값(`리뷰 3개, 트래픽 2개`)인지
2. **날짜 열** — `26-08-20` 형식 머리글이 날짜로 제대로 읽히는지, 그리고
   그 값이 **연-월-일**이 맞는지 (일-월-연이면 `TWO_DIGIT_YEAR_FIRST`를 `false`로)

출력을 그대로 보내주시면 맞는지 확인해 드리겠습니다.
